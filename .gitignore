# Build trees
/build/
/build-cmake/
/build-wasm/

# WASM artifacts are regenerated by build_wasm.sh; commit deliberately if desired
# web/geo.js
# web/geo.wasm

# Node / Astro (UI layer)
node_modules/
dist/
.astro/

# Editor / LSP
.cache/
compile_commands.json

# OS
.DS_Store
